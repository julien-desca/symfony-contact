{% extends 'base.html.twig' %}

{% block body %}
    {{ form_start(form) }}
    {{ form_row(form.nom) }}
    <input type="submit" class="btn btn-dark btn-block" value="Enregistrer"/>
    {{ form_end(form) }}
{% endblock %}